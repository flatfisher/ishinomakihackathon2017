{
  "name": "actions-irkit",
  "description": "HomeIRKit",
  "version": "0.0.1",
  "private": true,
  "license": "Apache Version 2.0",
  "author": "Google Inc.",
  "engines": {
    "node": "~4.2"
  },
  "scripts": {
    "lint": "semistandard --fix \"**/*.js\"",
    "start": "functions deploy HomeIRKit --trigger-http",
    "deploy": "gcloud beta functions deploy IRHomeIRKit --trigger-http --stage-bucket name"
  },
  "dependencies": {
    "actions-on-google": "^1.1.0",
    "request" : "^2.76.0"
  },
  "devDependencies": {
    "semistandard": "^9.1.0"
  }
}