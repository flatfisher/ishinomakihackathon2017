{
  "name": "actions-irkit",
  "description": "IRKit",
  "version": "0.0.1",
  "private": true,
  "license": "Apache Version 2.0",
  "author": "Google Inc.",
  "engines": {
    "node": "~4.2"
  },
  "scripts": {
    "lint": "semistandard --fix \"**/*.js\"",
    "start": "functions deploy IRKit --trigger-http",
    "deploy": "gcloud beta functions deploy IRKit --trigger-http --stage-bucket <name>"
  },
  "dependencies": {
    "actions-on-google": "^1.1.0"
  },
  "devDependencies": {
    "semistandard": "^9.1.0"
  }
}